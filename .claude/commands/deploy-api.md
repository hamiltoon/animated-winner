---
description: Deploy the GraphQL API to Cloudflare Workers
---

Deploy the API to Cloudflare Workers.

Steps:
1. Navigate to the api directory
2. Run `npx wrangler deploy`
3. Verify deployment succeeds
4. Test the API at https://recipe-saver-api.hamiltoon.workers.dev/graphql
